// `theme.parts`: the default renderers as a MODULE, so `theme.parts.totals(ctx, view)`
// can be called directly (eject: start from the default, then edit).
#import "frame.typ": (
  bank-account, company, contact, continuation, logo, marks, page-number,
  qr-bill, recipient, reference-list, references, registration, return-address,
  sender, sender-details, title,
)
#import "body.typ": (
  bank-details, items-table, line-items, notes, payment-terms, signature,
  totals,
)
