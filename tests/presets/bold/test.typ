/// [ppi: 60]

// Gallery of the `bold` preset: a brand and motion studio, Berlin (a4-digital), on the layout
// the preset picks for its sender (layout: auto). Data: tests/theme/gallery/bold.typ.
#import "/tests/theme/gallery/bold.typ": gallery

#gallery()
