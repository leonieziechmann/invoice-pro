/// [ppi: 60]

// Gallery of the `compact` preset: an industrial wholesaler's collective invoice (a4-dense), on the layout
// the preset picks for its sender (layout: auto). Data: tests/theme/gallery/compact.typ.
#import "/tests/theme/gallery/compact.typ": gallery

#gallery()
