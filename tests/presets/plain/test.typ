/// [ppi: 60]

// Gallery of the `plain` preset: a freelance translator under the small-business rule (plain), on the layout
// the preset picks for its sender (layout: auto). Data: tests/theme/gallery/plain.typ.
#import "/tests/theme/gallery/plain.typ": gallery

#gallery()
