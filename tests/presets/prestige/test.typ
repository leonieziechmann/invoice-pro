/// [ppi: 60]

// Gallery of the `prestige` preset: a boutique hotel's guest folio, Vienna (a4-band), on the layout
// the preset picks for its sender (layout: auto). Data: tests/theme/gallery/prestige.typ.
#import "/tests/theme/gallery/prestige.typ": gallery

#gallery()
