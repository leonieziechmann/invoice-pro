/// [ppi: 60]

// Gallery of the `boxed` preset: an electrician's invoice to a private customer (DIN 5008 A), on the layout
// the preset picks for its sender (layout: auto). Data: tests/theme/gallery/boxed.typ.
#import "/tests/theme/gallery/boxed.typ": gallery

#gallery()
