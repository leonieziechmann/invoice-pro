/// [ppi: 60]

// Gallery of the `soft` preset: a café and bakery with catering (a4-digital), on the layout
// the preset picks for its sender (layout: auto). Data: tests/theme/gallery/soft.typ.
#import "/tests/theme/gallery/soft.typ": gallery

#gallery()
