invoice::invoice-nr is missing; every invoice needs a unique, sequential number (EN 16931 BT-1)
invoice-pro found 3 problems (validation: "strict"; preview them with validation: "draft" or --input invoice-pro-validation=draft): 1. invoice::invoice-nr is missing; every invoice needs a unique, sequential number (EN 16931 BT-1) 2. invoice::sender has neither `vat-id` nor `tax-nr`; the supplier's VAT ID or tax number is required (EN 16931 BT-31, BT-32) 3. invoice::recipient has no address (`address`, `city`); the recipient's full address is required (EN 16931 BG-8)
e-invoicing (profile 'en16931') requires a buyer electronic address (BT-49). Set 'electronic-address', 'vat-id', or 'email' on the recipient.
invoice-pro found 2 problems (validation: "strict"; preview them with validation: "draft" or --input invoice-pro-validation=draft): 1. invoice: the document has no line items; an invoice must state the quantity and kind of the supply (EN 16931 BG-25) 2. e-invoicing (profile 'en16931') requires a buyer electronic address (BT-49). Set 'electronic-address', 'vat-id', or 'email' on the recipient.
invoice::recipient::vat-id is missing, but the invoice applies reverse charge or an intra-community supply; the recipient's VAT ID is required (EN 16931 BT-48)
theme::layout::areas::footer is 42.2mm tall, but the bottom margin leaves 17.4mm between footer-descent and the 5mm footer-clearance; use the computed margin (theme.custom.page(margin: (bottom: reset()))), raise margin.bottom or shorten the footer
invoice::invoice-nr is missing; every invoice needs a unique, sequential number (EN 16931 BT-1)
variable `invoice::validation`("visual") must be one of none, "draft", "strict". Did you mean "draft"?
--input invoice-pro-validation=strcit is not a validation level; use none, draft or strict. Did you mean `strict`?
theme::tokens::colors has unknown key `primry`. Did you mean `primary`? Allowed keys: primary, on-primary, primary-text, accent, accent-text, text, text-muted, border, tint, background
